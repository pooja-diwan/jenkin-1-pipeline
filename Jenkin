pipeline {
    agent any 
        stages {
            stage ('build') {
                steps {
                    echo "build started"
                    sleep 2
                    echo "done"
                }
           } 
        }
}
