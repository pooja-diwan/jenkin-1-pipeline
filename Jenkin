pipeline{
    agent any
    stages{
        stage('develop'){
            steps{
                echo 'build stage in progress'
                sleep 2
                echo 'build stage completed'
            }
        }
    }
}
